This repository contains the Google Collaboratory notebook in which multiple supervised machine learning models are trained on the FARS dataset from the NHTSA for the
purpose of predicting the severity of a car accident.
